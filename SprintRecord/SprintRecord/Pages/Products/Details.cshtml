@page
@model SprintRecord.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Sprints</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Sprints.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Sprints.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Sprints.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
